[tool.poetry]
name = "msgrpcenv"
version = "0.1.0"
description = ""
authors = ["NetDevs <ryan@netdevs.net>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.11"
pymetasploit3 = "^1.0.6"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
